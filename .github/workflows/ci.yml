name: CI Pipeline

on:
  push:
    branches:
      - '**'
    paths:
      - 'services/**'
      - '.github/workflows/ci.yml'
      - 'docker-compose.yml'
      - '.env.example'
      - 'docs/api/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'services/**'
      - '.github/workflows/ci.yml'
      - 'docker-compose.yml'
      - '.env.example'
      - 'docs/api/**'

jobs:
  base-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run pre-commit hooks
        uses: pre-commit/action@v3.0.1

  build-common:
    runs-on: ubuntu-latest
    needs: base-lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build common module
        run: mvn -f services/pom.xml -P common -pl common -am package

  java-services:
    runs-on: ubuntu-latest
    needs: [base-lint, build-common]

    strategy:
      matrix:
        service: [
          gateway,
          switch,
          authorization,
          card-management,
          terminal-simulator,
          merchant-acquirer,
          transaction-logger
        ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Run java linter
        run: mvn -f services/pom.xml -pl ${{ matrix.service }} verify -DskipTests

      - name: Build and run unit tests
        run: mvn -f services/pom.xml -P${{ matrix.service }} -pl ${{ matrix.service }} -am package

      - name: Build docker image
        run: docker build -f services/${{ matrix.service }}/Dockerfile -t ${{ matrix.service }}:latest .

  frontend-dashboard:
    runs-on: ubuntu-latest
    needs: base-lint

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        working-directory: ./services/dashboard
        run: npm install

      - name: Run ESLint
        working-directory: ./services/dashboard
        run: npm run lint

      - name: Run frontend tests
        working-directory: ./services/dashboard
        run: npm test -- --run

      - name: Build frontend application
        working-directory: ./services/dashboard
        run: npm run build

      - name: Build frontend docker image
        run: |
          docker build -f services/dashboard/Dockerfile -t dashboard:latest ./services/dashboard
