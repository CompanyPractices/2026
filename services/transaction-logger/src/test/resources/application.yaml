spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    table: transaction_logger_schema_history

websocket:
  allowed-origins: "*"
  ping-interval-ms: 30000
