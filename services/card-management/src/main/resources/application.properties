spring.config.import=optional:file:.env[.properties]
server.port=${PORT:8080}

# PostgreSQL
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:processing}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.jpa.hibernate.ddl-auto=update

# Logging
logging.level.com.processing=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Application
app.card-service.issuer-id=${ISSUER_ID:ZZZZZZ}

#Generator
app.card-service.min-balance=${MIN_BALANCE:1000000}
app.card-service.max-balance=${MAX_BALANCE:50000000}
app.card-service.min-daily-limit=${MIN_DAILY_LIMIT:5000000}
app.card-service.max-daily-limit=${MAX_DAILY_LIMIT:30000000}
app.card-service.currency-code=${CURRENCY_CODE:643}
